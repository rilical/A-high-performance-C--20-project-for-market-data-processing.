// *** AUTOGENERATED – DO NOT EDIT (run: python codegen/generate.py) ***

#pragma once

#include "messages.hpp"
#include "../../runtime/status.hpp"
#include "../../runtime/endian.hpp"
#include <cstddef>
#include <cstdint>
#include <cstring>

{%- set ns_parts = protocol.split('_') %}
{%- if ns_parts|length > 1 %}

namespace {{ ns_parts[0] }} { namespace {{ ns_parts[1] }} { namespace v{{ version }} {
{%- else %}

namespace {{ protocol }} { namespace v{{ version }} {
{%- endif %}

// Encoder for {{ protocol }} v{{ version }}
class Encoder {
public:
{%- if schema.get('messages', {}).get('LoginRequest') %}
    static market::runtime::status encode(const LoginRequest& m, uint8_t* out, size_t out_sz, size_t& written);
{%- endif %}
{%- if schema.get('messages', {}).get('NewOrderCross') %}
    static market::runtime::status encode(const NewOrderCross& m, uint8_t* out, size_t out_sz, size_t& written);
{%- endif %}
};

{%- if ns_parts|length > 1 %}
}  // namespace v{{ version }}
}  // namespace {{ ns_parts[1] }}
}  // namespace {{ ns_parts[0] }}
{%- else %}
}  // namespace v{{ version }}
}  // namespace {{ protocol }}
{%- endif %}
